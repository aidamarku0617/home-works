NestedLoop:
When we write a loop inside the another loop it is coled a nested loop.

Examples:
  for(int i=1; i<=5; i++){
  for(int j=1; j<=5; j++){
  system.outt.println("sorry")
  }
  }
  will be printed word sorry 25 times;

  Array:
  Array is a collection of similar items store in one single variable. these values
   can be stored and accessed using the indexes. an index is a number
    that is assigned to every itm we store in an array.

    Syntax:
    There are two ways of creating an array create and fill at the same time or
    create any empty array and use aloop
    DataType[] nameOfTheArray={item1,item2,item3..}
    for storing the names
    String[] names={"sino","naso","kendi"}
    int[] numbers={10,12,50}
    car[] genders={'M','F','U'}
    boolean[] conds={fasle,true,true,false}